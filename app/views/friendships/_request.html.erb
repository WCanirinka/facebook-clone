<%= form_for @friendship do |f|%>
  <div class="field">
        <%= f.hidden_field :user_id, value: user_id %>

  </div>
  <%= f.submit "Send Friend Request", class: "btn btn-primary d-none" %>
<% end %>
